# create-x402-app

A CLI tool to create a new x402 server, including a sovereign self-hosted facilitator.

## Usage

```bash
bun create x402-server <project-name>

# or
npm create x402-server <project-name>
```